<form method="post" action="{{ path('app_estimatedexpenses_delete', {'id': estimatedexpense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estimatedexpense.id) }}">
    <button class="btn">Delete</button>
</form>
