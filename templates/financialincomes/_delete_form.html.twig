<form method="post" action="{{ path('app_financialincomes_delete', {'id': financialincome.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ financialincome.id) }}">
    <button class="btn">Delete</button>
</form>
