



    <div class="table-wrapper" style="max-height:300px">
        <table class="my-table">
            <thead>
            <tr>

                <th >Topic Name</th>
                <th>Speaker Name</th>
                <th>Topic Description</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for topic in topics %}
                <tr onclick="location.href='{{ path('app_topics_show', {'id': topic.id}) }}'" style="cursor:pointer;">

                    <td style="">{{ topic.topicname }}</td>
                    <td>{{ topic.speakername }}</td>
                    <td style=" max-width: 180px;padding-right: 20px /* Adjust this value as needed */
        ">{{ topic.topicdescription }}</td>

                    <td>
                        <a class="edit-button" href="{{ path('app_topics_edit', {'id': topic.id}) }}">  <img src="{{ asset('img/editIcon.svg') }}" alt="Edit Icon"></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">No Topics Found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>



