<form method="post" action="{{ path('app_logistic_delete', {'id': logistic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logistic.id) }}">
    <button class="btn">Delete</button>
</form>
