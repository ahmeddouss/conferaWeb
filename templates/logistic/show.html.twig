{% extends 'base.html.twig' %}

{% block title %}Logistic{% endblock %}

{% block content %}
    <h1>Logistic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logistic.id }}</td>
            </tr>
            <tr>
                <th>Providedlog</th>
                <td>{{ logistic.providedlog }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ logistic.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a class="back-button" href="{{ path('app_logistic_index') }}">Back to list</a>


    <a href="{{ path('app_logistic_edit', {'id': logistic.id}) }}">edit</a>

    {{ include('logistic/_delete_form.html.twig') }}
{% endblock %}
